===> Authentication
    =>register( 1.user ka data save karna, 2. token user ko dena )
    =>login
    =>logout
    =>[OTP Based Registration]

===> Post
    => Create
    => can see the feed
    => like posts
    => save post 

===> users
    => followers
    => following

user = {
    username:String,
    email:String,
    password:String,
    bio:String,
    followers:Array,
    profile_img:String
}

post = {
    caption:String,
    img_url:String,
    user:userId,
    createdAt:Date-time,
}